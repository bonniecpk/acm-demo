apiVersion: v1
kind: Service
metadata:
  name: whereami
  namespace: replace-me
spec:
  selector:
    app: whereami
  ports:
  - port: 8080
    targetPort: 8080
    appProtocol: http
    name: http-access
